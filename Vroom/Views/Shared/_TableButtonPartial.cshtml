@model int
@{
}
<td class="text-right">
	<div class="btn-group" role="group" action="Edit">
	<!--	@{
			//var controllername = @ViewContext.RouteData.Values["controller"].ToString();
		}
	//if(controllername.Equals("Make"))
	{
		<a type="button" class="btn btn-primary" href="@Url.Action("Edit","Make",new{Id=Model})"><i class="fa-solid fa-pen-to-square"></i></a>
	}
	//else
	{
		<a type="button" class="btn btn-primary" href="@Url.Action("Edit","Model",new{Id=Model})"><i class="fa-solid fa-pen-to-square"></i></a>
	}-->
	<a type="button" class="btn btn-primary" href="@Url.Action("Edit",new{Id=Model})"><i class="fa-solid fa-pen-to-square"></i></a>
	<form method="post">
		<button onclick="if(!confirm('Are you sure you want to delete this item?')){return false}" type="submit" class="btn btn-danger rounded-0" asp-route-id=@Model asp-action="Delete"><i class="fa-solid fa-trash-can"></i></button>
	</form>
	</div>
</td>